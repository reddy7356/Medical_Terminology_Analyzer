# Medical Terminology Analyzer Environment Configuration

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================

# Neon PostgreSQL Connection String (REQUIRED for database features)
# 
# How to get your Neon connection string:
# 1. Sign up for free at: https://neon.tech
# 2. Create a new project in the Neon console
# 3. Navigate to "Connection Details" in your project dashboard
# 4. Copy the connection string (it will look like the example below)
# 5. Make sure to enable the pgvector extension in your Neon database:
#    - Run: CREATE EXTENSION IF NOT EXISTS vector;
#
# Format: postgresql://[user]:[password]@[endpoint]/[database]?sslmode=require
# Example: postgresql://alex:AbC123xyz@ep-cool-darkness-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
#
# Note: If this is not set, the system runs in analysis-only mode (no database persistence)
NEON_CONNECTION_STRING=postgresql://username:password@your-neon-host/your-database?sslmode=require&channel_binding=require

# ==============================================================================
# AI/ML CONFIGURATION
# ==============================================================================

# OpenAI API Key for vector embeddings (OPTIONAL)
#
# How to get your OpenAI API key:
# 1. Sign up at: https://platform.openai.com/signup
# 2. Navigate to: https://platform.openai.com/api-keys
# 3. Click "Create new secret key" and copy it
# 4. Paste it below (it starts with "sk-")
#
# The system uses OpenAI's text-embedding-3-small model (1536 dimensions)
# Cost: ~$0.02 per 1M tokens (very affordable for this use case)
#
# Note: If not set, the system automatically falls back to deterministic SHA-256
#       embeddings, maintaining full functionality without API costs
OPENAI_API_KEY=your-openai-api-key-here

# ==============================================================================
# WEB SERVER CONFIGURATION
# ==============================================================================

# Flask web server host (0.0.0.0 allows external connections, localhost = local only)
FLASK_HOST=localhost

# Flask web server port (default: 5556)
# The system will automatically find an available port if this one is in use
FLASK_PORT=5556

# Enable debug mode (set to False in production)
DEBUG=True

# ==============================================================================
# VECTOR DATABASE SETTINGS
# ==============================================================================

# Vector embedding dimension (must match OpenAI's text-embedding-3-small: 1536)
VECTOR_DIMENSION=1536

# Similarity threshold for vector searches (0.0 to 1.0, higher = more similar)
SIMILARITY_THRESHOLD=0.7

# Maximum number of results to return from similarity searches
MAX_RESULTS=100

# ==============================================================================
# MEDICAL TERMINOLOGY SETTINGS
# ==============================================================================

# Minimum frequency for a term to be considered significant
MIN_TERM_FREQUENCY=2

# Maximum number of terms to extract per medical case
MAX_TERMS_PER_CASE=50

# Medical terminology categories (DO NOT MODIFY unless extending the system)
TERMINOLOGY_CATEGORIES=cardiovascular,respiratory,neurological,gastrointestinal,endocrine,infectious,orthopedic,oncology,psychiatric,renal,hematology,dermatology,ophthalmology
